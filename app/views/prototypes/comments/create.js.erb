$('#comment_field').val('')
$('#comments_count').html(
  '<%= j(" (#{@prototype.comments_count}) ") %>'
);
$('#comments').html(
  '<%= j(render partial: 'prototypes/comment', collection: @prototype.comments_of_users) %>'
);
